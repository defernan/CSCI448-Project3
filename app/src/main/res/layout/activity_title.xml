<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="csci448.connectfour.TitleActivity">

    <csci448.connectfour.TitleView
        android:id="@+id/titleBackground"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"  />

    <Button
        android:id="@+id/buttonSinglePlayer"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/buttonTwoPlayer"
        android:text="Single Player"
        android:onClick="singlePlayerClicked"/>

    <Button
        android:id="@+id/buttonTwoPlayer"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/buttonExit"
        android:text="Two Player"
        android:onClick="twoPlayerClicked"/>
    <Button
        android:id="@+id/buttonExit"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/titleBackground"
        android:text="Exit Game"
        android:onClick="exitClicked"/>


</RelativeLayout>
